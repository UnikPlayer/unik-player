{
  "scripts": {
    "build:version": "node build-version.js",
    "build:all": "npm run build:version && cd frontend && npm run build && cd ../backend-csharp/UnikPlayer && dotnet publish -c Release && cd ../../projBuild && makensis installer.nsi"
  }
}